---
import BaseLayout from '@components/layout/BaseLayout.astro';
import { getConfig } from '@lib/config';

const config = getConfig();
if (!config.pages.travel) return Astro.redirect('/404');
---

<BaseLayout title="Travel" description="Conference and fieldwork travel">
  <div class="animate-on-scroll">
    <h1 class="text-3xl font-bold mb-4">Travel Map</h1>
    <p class="text-[var(--color-text-secondary)] mb-8">Places I have visited for conferences, workshops, and fieldwork.</p>
  </div>
  <div class="rounded-xl border border-[var(--color-border)] bg-[var(--color-card-bg)] p-8 text-center text-[var(--color-text-secondary)] animate-on-scroll">
    <svg class="w-16 h-16 mx-auto mb-4 opacity-30" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
    <p>Interactive travel map coming soon. For now, add locations to <code class="text-xs bg-[var(--color-border)] px-1.5 py-0.5 rounded">src/content/travel.yml</code>.</p>
  </div>
</BaseLayout>
